//export 1 obt with all our functions 

const head = require("./head");
const tail   = require('./tail');
const middle = require('./middle');

module.exports = {
    head,
    tail,
    middle
};

//console.log(module);